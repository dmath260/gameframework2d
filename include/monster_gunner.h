#ifndef __MONSTER_GUNNER_H__
#define __MONSTER_GUNNER_H__

#include "monster.h"

/**
* @brief populate a gunner with data
* @param self the entity to finish populating
*/
void monster_gunner_populate(Entity *self);

#endif#pragma once
