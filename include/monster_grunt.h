#ifndef __MONSTER_GRUNT_H__
#define __MONSTER_GRUNT_H__

#include "monster.h"

/**
* @brief populate a grunt with data
* @param self the entity to finish populating
*/
void monster_grunt_populate(Entity *self);

#endif#pragma once
